trigger:
- main

pool:
  name: 'apname'  # Replace 'AzureVM' with your self-hosted agent pool name

steps:

# Step 1: Install dependencies using pip
# Step 2: Run the Flask app
- script: |
    python --version
    python app.py
  displayName: 'Run Flask app'
